// Write a constructor vector in 2D - takes x & y parameters which should save to properties of the same name.

function Math (x, y) {
  this.x === x
  this.y === y
}

// var addMath = new Math (x + y)
// var subtractMath = new Math (x - y)



// give vector prototype 2 methods (plus & minus) that take another vector as parameter
